publish-to-wiki:
  stage: publish
  rules:
    - if: '$CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE != "schedule"'
(the only change: && $CI_PIPELINE_SOURCE != "schedule")
While you're in there, also swap the guard to the content-based version so the deadlock can't recur:
yaml    - |
      if ! diff -rq wiki /tmp/wiki-clone -x .git -x .gitlab > /dev/null 2>&1; then
        echo "ERROR: wiki has un-synced UI edits. Run the sync schedule, merge its MR, retry."
        exit 1
      fi
